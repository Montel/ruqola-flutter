name: libddpapi
description: ddpapi for Rocketchat client
version: 1.0.0
# repository: https://github.com/my_org/my_repo

publish_to: 'none'
environment:
  sdk: ^3.5.3

# Add regular dependencies here.
dependencies:
  crypto: ^3.0.6
  web_socket_client: ^0.1.5
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
